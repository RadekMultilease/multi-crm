apps:
  frontend:
    image:
      repository: radeczu/angular-sandbox
      tag: arm64
      replicas: 1
    service:
          type: LoadBalancer
          ports:
            - name: status
              port: 8080
            - name: dev
              port: 4200
    persistence:
      enabled: true
      claimName: frontend-pvc
      size: "2Gi"
    ingress:
      enabled: true
      host: dev.crm.pi
      ingressPort: 4200
      uploadLimit: "50m"
    resources:
      requests: { cpu: "100m", memory: "256Mi" }
      limits: { cpu: "500m", memory: "512Mi" }

  # apitestapp:
  #   image:
  #     repository: radeczu/apitestapp
  #     tag: latest
  #   service:
  #     type: ClusterIP
  #     ports:
  #       - name: api
  #         port: 2137
  #   ingress:
  #     enabled: false