apiVersion: v1
kind: Service
metadata:
  name: postgres-db
spec:
  externalTrafficPolicy: Local
  selector:
    app: postgres
  ports:
    - protocol: TCP
      port: 5432        
      targetPort: 5432   
      nodePort: 30432    
  type: NodePort         